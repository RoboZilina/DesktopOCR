# VN Golden Frames

Placeholder directory for VN regression assets. Add representative frames (PNG) plus expected OCR outputs (JSON/TXT)
to lock the stable pipeline. The `run_vn_goldens.py` helper will consume these files once implemented.
