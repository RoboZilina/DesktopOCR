# DesktopOCR dependencies
# Target runtime: Python 3.11.x on Windows

# WinRT capture bindings (aligned with current code/imports)
winsdk==1.0.0b10

# OCR + image processing
opencv-python==4.13.0.92
numpy==2.4.4
onnxruntime-directml==1.24.4

# UI / async integration
PyQt6==6.11.0
qasync==0.28.0
aiohttp==3.9.5

# Desktop UX helpers

# TTS — cloud fallback (zero local size, requires internet)
edge-tts==7.0.0
pygame==2.6.1
sounddevice==0.5.2

# Offline translation (JA→EN fallback when no internet)
argostranslate==1.9.6

# Packaging
pyinstaller
